---
language: node_js
node_js:
  - "6.7.0"

sudo: false

cache:
  directories:
    - node_modules

before_install:
  - "npm config set spin false"
  - "npm install -g npm@3.10.3 bower brunch@1.7.20"

install:
  - npm install
  - bower install

script:
  - brunch build --production
